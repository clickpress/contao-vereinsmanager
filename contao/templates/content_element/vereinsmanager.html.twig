{% extends "@Contao/content_element/_base.html.twig" %}

{% set attributes = attrs()
    .addClass("#{type}")
    .mergeWith(attributes|default) %}

{% set searchable = false %}

{% block content %}
    {% set source = data.vereinsmanager_url %}
    {% do csp_source('frame-src', source) %}
    <iframe
            data-scroll-behavior="iframe"
            data-scroll-offset="150"
            data-smooth-scroll="true"
            id="vereinsmanager-iframe-1"
            src="{{ source }}/tourenplan/"
            style="width: 100%; border: none; min-height: 400px;">
    </iframe>

    <script src="{{ data.vereinsmanager_url }}/static/scripts/vm_iframe_resizer.js"></script>
{% endblock %}
